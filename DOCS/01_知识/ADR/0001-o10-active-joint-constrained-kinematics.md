---
status: accepted
---

# O10 IK 以主动关节为唯一求解空间

OmniHand O10 重定向只在 10 个真实可控的主动关节上求解，并使用版本固定、左右手独立的 MJCF 耦合多项式显式派生 6 个被动关节后再计算完整运动学。项目不采用 URDF 线性 `mimic` 作为耦合权威，也不允许优化器把 16 个关节作为独立变量后仅用软损失逼近耦合，因为这两种方式都可能让求解时预测的指尖姿态与实体手接收同一组 10 维命令后能够实现的姿态不一致；具体 FK 和优化器后端保持可替换。
