# 归因 ⑩（✅ 第四次确认）：超限根源 = 机械零位本来就在配置下限外

> **结论：✅ 确认并修复。** 这只手的 pinky_abad 真实零位 -0.1861，配置下限 -0.1850（差 0.0011 弧度）——放宽下限到 -0.19 后全链路打通。**4 个叠加问题全部解决。**

## 1. 观测（看到了什么）

- 读数稳定在 -0.186104（归因⑨ 已验证数据干净）。
- 配置下限是 -0.1850049…，读数比下限还低 0.0011 弧度。
- 也就是说：**这只手一开机、一动没动，就已经"超限"了。**

## 2. 推测（重新定位）

**不是读数异常，是这只手的机械零位本来就落在配置限位之外。**

```
配置下限:  -0.1850 ───────────────┐
                                  ▼
真实零位:  -0.1861  ← 天生就在线外 0.0011 弧度
```

-0.1850 这个下限是"理论值"；这只手实际装出来的零位是 -0.1861。理论限位和真实机械之间存在偏差。

## 3. 验证（怎么证明）

- 确认实测零位：多次采样稳定 -0.186104（可信）。
- 修复：LEFT pinky_abad 下限从 `-0.1850049007113989` 放宽到 `-0.19000000000000000`（留出 0.004 余量）。
- 同步更新 `test_joints.py` 的限位断言，重新跑全部测试。

## 4. 结果（验证怎么说）

- ✅ 限位检查通过，read 服务不再报超限。
- ✅ 全部测试通过（contracts 103 + control 34 + hardware 23）。
- ✅ **全链路最终状态**：
  ```
  arm 成功 → 手可运动 → 反馈就绪 → 限位通过
  4 个问题全部修复，真机验证通过
  ```

## 5. 结论与教训

**硬件的"真实零位"优先于配置里的"理论限位"。** 每只手装配公差不同，理论限位必须用实测数据校准，而不是让硬件去迁就配置。

> 教训⑩：**实测校准数据要回写配置。** "差一点点"（0.0011 弧度）的问题，看起来最小，却会卡住整条链路——小偏差 + 严格检查 = 大故障。

## 6. 收尾 · 整个故事

从"arm 失败"到"全链路打通"，真相是 **4 个问题层层叠加**，单独看每个都"不太像大问题"，串起来就是"手完全不可控"：

1. SDK 批量错误接口 bug（读数骗人）
2. 系统把 commu_except 历史标记当致命故障锁存（加工逻辑错）← 最初问的核心
3. provider 从不发布反馈（架构缺口）
4. control 命令缺 name（消息契约违约）

回顾全部链路请回 [INDEX](INDEX.md)。
