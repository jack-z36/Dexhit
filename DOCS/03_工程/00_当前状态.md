# 当前状态

更新时间：2026-08-10

## 已完成

- 第一版根目录、`DOCS/` 分类、源码阶段入口和 Skill 入口已建立。
- 四阶段主链固定为 `collection → raw MCAP → processing → LeRobotDataset v3 → training → ACT bundle → deployment`。
- 当前权威规则压缩为用户概念体系、编程执行、文档体系三类。
- Skill 已归一为 `skills/` 单一实体，两个兼容入口使用相对符号链接。

## 当前空白

- `src/` 只有阶段 README，没有业务源码、ROS 包、脚本、schema 或第三方依赖。
- 尚未定义具体采集设备、消息字段、LeRobotDataset 特征、ACT bundle 格式或 deployment 运行协议。
- 尚未创建数据集、模型、日志或运行缓存；这些属于后续生成物。

## 下一步入口

下一项工程工作应先在对应阶段 README、知识页和状态页中确认边界，再添加最小实现与验证。具体任务状态写入本目录或后续阶段工作台，不把稳定规则复制到这里。
