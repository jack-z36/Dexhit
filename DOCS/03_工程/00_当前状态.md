# 当前状态

更新时间：2026-08-14

## 当前判断

Rokoko → OmniHand O10 已按 T01 重新冻结 wire contract，并完成 T01–T11 的代码、自动化门禁、真实模型/SDK 外部验证以及 A14/A21 架构 reviewer 审查。当前仍不能宣称真实 Rokoko 现场兼容、真机行为、速度舒适性或 watchdog 已验证。

## 已验证

- T01 接口包使用全新 Python 3.12 ROS 安装空间重建，schema/smoke 5/5 通过；`RetargetingState` 为数组式 schema，没有独立 `PHASE_FROZEN`，并显式拥有 `SOLVER_NOT_RUN=0`。
- T02 `omnihand_o10_contracts`：103 tests 通过；主动关节名称、索引和精确限位由该包单一拥有。
- T03 `omnihand_o10_model`：78 tests 通过；ament 安装空间/外部 fixture loader、MJCF/URDF 结构校验和 provenance/SHA-256 工具已实现。
- T04 `rokoko_hand_receiver`：包内 22 tests 通过；只证明官方文档构造 fixture、loopback 和 QoS 兼容，真实 Rokoko Studio UDP 抓包尚未提供。
- T05 `hand_retargeting`：基础 package tests 与 ROS 图测试通过，包含 collecting → waiting-first-valid-IK、左右隔离、掌坐标退化、长度异常和 MODEL_ERROR；冻结事实由逐指 `length_state` 表达。
- T06：真实固定 commit 左右 URDF/MJCF 的 provenance/hash/结构校验通过，左右 `nq=nv=16`；mamba Python 3.12 环境下真实五指 Jacobian/目标梯度中心差分 4/4 通过；T06 targeted/entrypoint tests 为 16 passed、1 skipped。生产入口显式接入工作区外 mamba 依赖和外部 fixture 后，公开 ROS command 验证为 56 条、每条 10 个主动关节。
- T07 控制包 65 tests 通过，ROS 图测试 6/6 通过；arm/disarm/clear_fault 幂等、拒绝优先级、双轴 freshness、PAUSED_TARGET_STALE、错误锁存、回读超时、限速和软件 Provider 均有证据。
- T08：正式 Agilink SDK v1.1.8 外部只读源码/CPython wheel 已核对并安装到工作区外 mamba 环境；Provider/production composition、六类 factory kwargs、fresh read/no-cache 和 fail-fast 契约共 23/23 通过。未调用真实 factory 或真机。
- T09 production launch 结构测试 2/2 通过；只包含三个业务节点、生产 Adapter 和外部 vendor launch，参数文件、vendor launch 和两侧 Provider transport 参数显式必填；launch 打包已通过干净 colcon 构建。
- T10：真实模型/Pinocchio/NLopt mamba runtime 下公共 ROS 图 21/21 通过，包含 UDP → 接收 → IK → 控制 → 软件 Provider 命令路径、故障注入、MCAP 和吞吐；未执行真机 Provider。
- T11 架构门禁：A01–A13、A15–A20、A22 共 20/20 通过；A14/A21 manual reviewer checklist 均 PASS。
- 10 个 ROS 包可在工作目录外临时 build/install 空间干净构建；最近一次全包 `colcon test` 为 278 个 colcon 测试项、0 failures、0 errors、2 skips。真实外部 runtime 验证另行覆盖 T06/T10 成功路径。

## 外部阻断与未验证

- 环境按要求使用 `/home/hit/miniforge3/bin/mamba`，目标环境位于 `/home/hit/miniforge3/envs/dexhit_collection`；国内镜像直连超时后使用 Fclash `127.0.0.1:7890` 成功安装 Python 3.12.13、Pinocchio 4.1.0、NLopt 2.11.0，并安装外部 Agilink `omnihand` 1.1.8 wheel。
- `hand_retargeting_node` 不再直接使用缺少数值库的 `/usr/bin/python3`；安装后的 wrapper 要求显式设置工作区外 `DEXHIT_COLLECTION_PREFIX` 和 `OMNIHAND_O10_MODEL_FIXTURE`，缺失时返回 `BLOCKED_ENV`，不静默进入 `PHASE_MODEL_ERROR`。外部 fixture 使用 `provenance.json`，资产仍为 Manus 固定 commit 的只读 symlink。
- `ManusOmniBridge` 外部只读 clone 固定于 `f4fd0d913c2151bcb4be0d29fbc02761b9638009`，真实资产 provenance/hash 已通过；资产未复制进仓库，许可未确认状态保持不变。
- Agilink SDK 外部只读源码固定于 commit `026740d9fdd8ba32b0605fa702a992b322076f1b`；正式 factory/错误位的物理语义、真机反馈和真机动作仍未验证。
- A14/A21 已由 Kepler (`gpt-5.6-luna`) PASS 并记录 working-tree revision；真实 Rokoko Studio 脱敏 UDP 抓包仍未提供。
- 真实 Rokoko Studio 脱敏 UDP 抓包、真机反馈物理语义、速度舒适性、watchdog 和现场行为均未验证。
- 交接现场姿态的拇指目标曾在当前安全关节边界外，归一化残差约 `0.225`；该输入按设计保持无 command。使用真实模型生成的可达无真机 `RawHandFrame` 已验证 command 发布，不能把两种结果混写。

## 工作树与操作边界

- 未 commit、未 push、未执行真机动作。
- 生成 build/install/log/test-result 均使用 `/tmp` 或 Miniforge 工作目录外路径；未使用工作树旧生成接口作为验收依据。

## 测试文档入口

- [测试总览与追踪矩阵](03_测试总览与追踪矩阵.md)：T01–T11 的统一测试索引和全局门禁。
- [接口、模型与算法契约测试](04_接口模型与算法契约测试.md)：wire、模型和数值契约证据。
- [组件与 ROS 图行为测试](05_组件与ROS图行为测试.md)：接收、重定向和控制行为证据。
- [系统集成与架构门禁测试](06_系统集成与架构门禁测试.md)：Provider、系统图和架构门禁证据。
