# Dexhit Context

Dexhit 面向从人类示范到机器人执行的数据与控制链路。本词汇表约束项目特有概念，不记录实现接口、算法参数或阶段进度。

## Language

**实时手部遥操作系统**：
操作者佩戴 Rokoko Smartgloves，以自己的手指动作实时驱动 OmniHand O10 主动手指关节运动的系统。当前第一版不控制机械臂或腕部，也不包含高精度离线重定向、Teacher 数据生成或神经网络重定向。
_Avoid_: 手套控制程序、实时 Retargeter

**Rokoko 接收节点**：
实时手部遥操作系统中拥有 Rokoko 数据接入边界、向下游提供原始人体手部帧的模块。它不解释 OmniHand O10 的关节语义。
_Avoid_: Rokoko 发送指令节点、Rokoko 控制节点、Rokoko Bridge

**Rokoko 原始手部帧**：
Rokoko Studio 对一个逻辑手侧给出的完整 21 节点名称、位置和四元数集合，经接收节点按名称重排为项目规范顺序后，以 `RawHandFrame` 在左右独立 Topic 发布。它保留源数据语义，不包含掌心相对化、坐标轴映射、尺度归一化、特征裁剪或机器人关节目标。
_Avoid_: 归一化手部数据、指尖目标、Rokoko 关节角

**人体指尖目标**：
从 Rokoko 原始手部帧提取的五个指尖位置，并表达在由 `Hand` 原点与四指根部几何逐帧构造的人体解剖学手掌坐标系中。它已经排除人体手腕的世界平移和旋转，但尚不是 O10 指尖目标。
_Avoid_: 世界坐标指尖、O10 指尖目标、机器人关节目标

**人体解剖学手掌坐标系**：
以 Rokoko `Hand.position` 为原点、以掌根到四指根部中心为 `+Y`、以左右手各自规定的掌宽方向为 `+X`，并以 `+X × +Y` 定义 `+Z` 的逐帧坐标系。它由骨架位置构造，不使用 Rokoko `Hand.rotation` 决定朝向。
_Avoid_: Rokoko 世界坐标系、Rokoko Hand quaternion 坐标系

**规范 O10 手掌坐标系**：
右手固定为 O10 模型的 `R_palm` frame，左手固定为 `L_palm` frame；所有 FK 后端必须把结果适配到对应规范坐标系后再参与重定向。
_Avoid_: MuJoCo world frame、Pinocchio backend frame、仿真场景坐标系

**O10 指根**：
某根 O10 手指运动链第一个关节的原点，表达在同侧规范 O10 手掌坐标系中并由固定模型在零关节状态下计算。它不是掌心原点或可手工配置的偏移点。
_Avoid_: 指尖基准点、手写指根偏移

**O10 指链长度**：
固定 O10 模型在零关节状态下，从该指运动链首关节原点开始，经后续关节原点到 tip frame 原点的相邻线段长度之和。它不是指根到指尖的直线距离。
_Avoid_: 指根—指尖弦长、人工标定长度

**O10 几何参数源**：
O10 指根、指链长度和手掌方向映射均从专用无节点 ROS 资产包内版本锁定的左右 URDF 计算；Pinocchio 显式关闭线性 `mimic` 并保留 16 维完整关节。相同资产包中的左右 MJCF 只提供主动—被动关节耦合多项式。运行时不依赖外部绝对路径，也不并列维护手写几何参数表。
_Avoid_: 多模型混用、复制的运行时几何常量

**O10 核心模型资产**：
Phase 1 在线重定向必须安装的最小模型集合：左右生成态 URDF、左右 MJCF 和包含来源 commit、逐文件 SHA-256 及许可信息的 provenance 清单。Xacro、碰撞模型、MuJoCo 场景、STL 与 RViz 配置不属于核心运行依赖；未完成再分发许可确认的上游文件不得正式进入项目资产包。
_Avoid_: 外部参考仓库路径、完整仿真场景、硬件 SDK

**手掌方向映射**：
将人体解剖学手掌坐标中的向量映射到同侧规范 O10 手掌坐标的固定正交矩阵 `A_s`。它只依赖逻辑手侧和固定 O10 模型，一侧五指共享，不依赖操作者、数据帧或 FK 工具。
_Avoid_: 单指方向映射、FK 后端坐标适配

**运行期人体尺度**：
进程每次启动后，分别从一侧五根手指的合法骨架帧估计并冻结本次运行使用的人体指链长度；该侧五指全部冻结前不输出控制目标。一次运行只对应同一操作者和同一 Rokoko Actor Profile，冻结值在进程结束前保持不变。
_Avoid_: 用户手型档案、运行中换人检测、人工姿势校准

**单指有效目标**：
某根手指最近一次满足关节限位、求解终止和残差标准的 O10 主动关节目标。后续该指求解失败时保持此目标，其他手指仍可独立更新，并显式报告失败。
_Avoid_: 当前最佳但未达标解、关节极限替代解

**单指 IK 初值**：
某根手指一次 IK 求解开始时使用的主动关节候选；本次运行首次求解使用关节范围中点，后续使用该指上一有效目标。它是优化搜索起点，不是未经验证即可发送的控制命令。
_Avoid_: 随机初值、启动命令

**单指求解终止原因**：
单指优化器停止搜索的原因，例如达到内部收敛条件、评估次数上限、时间上限或求解器错误。它不单独决定当前候选能否进入控制链。
_Avoid_: IK 有效性、控制成功状态

**单指候选有效性**：
优化停止后对当前最佳候选进行的独立判定；只有求解器无异常、数值有限、主动关节合法且归一化残差达标时，候选才成为单指有效目标。
_Avoid_: 优化器正常返回、达到时间上限、内部收敛

**归一化指尖残差**：
O10 实际指尖与 O10 指尖目标的欧氏距离除以对应 O10 完整指链长度所得的无量纲比例，用于跨手指一致地判定 IK 候选质量。它不是人体手型归一化结果。
_Avoid_: 米制指尖误差、人体尺度比例、优化器返回码

**基础单指 IK 损失**：
归一化指尖残差的平方，是当前单指 IK 唯一确定的目标项；它不隐含指尖方向、关节变化、平滑、碰撞或多指联合惩罚。
_Avoid_: 未归一化位置平方误差、完整重定向评分

**单指有效运动学雅可比**：
在当前主动关节候选处，O10 指尖位置对该指主动关节角的导数。它由 Pinocchio 对完整指链给出的平移雅可比与 MJCF 主动—被动耦合映射的解析雅可比按链式法则相乘得到，不能通过简单抽取主动关节列代替。
_Avoid_: 完整关节雅可比、数值差分梯度、优化器搜索方向

**Phase 1 单指优化后端**：
以每根手指的主动关节为变量、以主动关节上下界为硬约束，使用 NLopt `NLOPT_LD_SLSQP` 最小化基础单指 IK 损失的局部梯度优化实现。实时路径使用解析梯度；中心数值差分只用于离线验证解析实现。
_Avoid_: 全局 IK、多初值搜索、16 关节独立优化

**重定向侧状态**：
手部重定向节点在 `/hand_retargeting/{side}/state` 发布的 `RetargetingState` 可录制观测。它区分状态发布时间、最近输入时间和求解完成时间，并逐指表达尺度、IK、历史保持、残差和求解器结果，同时表达整体阶段、就绪、stale、恢复进度与本事件是否发布命令。它用于诊断和验收，不参与 O10 控制。
_Avoid_: O10 控制命令、普通运行日志

**重定向目标时间**：
组合 O10 关节目标继承的触发源 Rokoko 原始手部帧接收时间，用于计算数据年龄和端到端延迟。它不是 IK 求解完成时间；求解完成时间属于重定向侧状态或诊断。
_Avoid_: 发布时刻、求解完成时刻、Rokoko 源时间

**O10 软关节目标消息**：
手部重定向节点在 `/o10_control/{side}/command` 发布的 `JointState`。它以固定 10 个主动关节名称自校验顺序，`position` 表示软平滑后的 rad 目标，时间戳继承触发它的 RawHandFrame；它不是硬限速后的厂商命令。
_Avoid_: 未平滑 IK 解、最终发送命令、O10 反馈

**手侧控制就绪**：
同侧五根手指的运行期人体尺度均已冻结，且每根手指都至少产生过一次单指有效目标的状态。达到此状态前，该侧不输出组合控制目标。
_Avoid_: 部分手指就绪、默认关节补位

**冻结尺度异常**：
当前手指链长度与本次运行冻结长度的相对偏差超过配置阈值。普通单指异常只保持该指最近有效目标；异常若使四指根部掌坐标系无法构造，则整侧当前帧无效。
_Avoid_: 运行中自动重估、静默更换操作者尺度

**接收时间**：
Rokoko 接收节点在 ROS 主机上收到 UDP 数据包的本地时间，用于录制对齐、输入新鲜度判断和本机链路观测。它不等同于 Rokoko 数据产生时间。
_Avoid_: Rokoko 时间、动作发生时间

**源时间**：
Rokoko 数据源随帧提供的原始时间信息；只有在真实数据包中确认字段、类型和时基后才进入消息契约。它不得在时基未知时被转换或冒充为 ROS 时间。
_Avoid_: ROS 收包时间、同步时间

**手部重定向节点**：
实时手部遥操作系统中把人体手部帧转换成 OmniHand O10 关节目标的模块。特征提取、坐标映射、求解、关节映射和动作平滑是它的内部职责，不是独立系统节点；第一版启动不要求操作者执行规定手势完成会话校准。
_Avoid_: 关节转换节点、IK 节点

**O10 控制节点**：
实时手部遥操作系统中接收 OmniHand O10 关节目标、施加最终关节变化率硬限制并控制目标逻辑手的模块。它不解释人体动作，也不负责人体到机器人的重定向或自然度平滑。
_Avoid_: OmniHand 驱动、重定向节点

**O10 控制状态**：
O10 控制节点在 `/o10_control/{side}/state` 发布的 `O10ControlState` 可录制观测。它逐侧表达反馈、错误监控、目标就绪和新鲜度，操作者授权、锁存故障、运动许可、目标处理、硬限速介入、硬件错误位及关键事件时间。控制节点从自己的输入与本地状态独立作出安全判定，不依赖该状态消息或 `RetargetingState` 反向驱动控制。
_Avoid_: 控制输入、重定向状态、普通运行日志

**动作平滑**：
手部重定向节点在单指 IK 候选通过有效性判定并组合为 10 维主动关节目标之后，为减少输出抖动而进行的软关节目标处理。它不改变 Rokoko 原始 21 节点、人体归一化目标或 IK 损失，也不能替代 O10 控制节点的最终安全限制。
_Avoid_: 硬限速、安全门

**关节变化率硬限制**：
O10 控制节点以最后实际发送命令为运行基准、以本机单调时钟为时间基准，对每侧每个主动关节施加的最终命令变化率上限。限制器必须由独立读取的新鲜真实关节反馈初始化，并限制单次可累计的时间额度；任何上游都不能绕过。它不负责让动作更像人或更平滑。
_Avoid_: 低通滤波、动作平滑

**硬限制器初始化反馈**：
O10 硬件 Adapter 在不先改变手姿态的前提下独立读取并提供的一帧新鲜、合法的 10 维主动关节真实状态，用于初始化对应侧关节变化率硬限制器。没有它时该侧保持未初始化且不得发送命令。
_Avoid_: 首个上游目标、零位、关节范围中点、上次运行缓存

**操作者控制授权**：
每个逻辑手侧独立维护、进程启动时必为 false 的显式 `armed` 锁存。只有操作者在所有前置条件当前成立时主动请求，才可置为 true；失败的请求不会排队等待以后自动生效。解除授权只停止产生新命令，不自动张手、回零或断开硬件驱动。
_Avoid_: 技术就绪、数据恢复、硬件急停

**操作者控制操作**：
操作者通过逐侧 `arm`、`disarm` 和 `clear_fault` ROS Service 请求 O10 控制状态转换。三种操作共用一次性请求—应答类型，返回是否完成、机器可读结果码、操作者说明和操作后的同侧控制状态；失败请求不排队或自动重放。
_Avoid_: 控制 Topic、长时 Action、写入控制状态消息

**手侧运动许可**：
操作者控制授权、真实启动反馈就绪、硬件错误监控就绪、控制节点已验证过目标、最近合法目标仍新鲜且无锁存故障同时成立时，由控制节点本地派生出的瞬时条件。它不是另一个可独立设置的开关，也不依赖诊断用的重定向状态；任一前置条件不成立时该侧不得产生新硬件命令。
_Avoid_: `armed`、IK ready、设备已连接

**可自动恢复暂停**：
由人体数据、单帧重定向或输入 stale 等上游暂时异常造成的单侧停止发送状态。它保留既有操作者授权和硬限制器运行基准；只有满足相应恢复条件后才通过有界过渡恢复，不代表硬件安全状态已经失去可信度。
_Avoid_: 锁存故障、解除使能、硬件急停

**锁存控制故障**：
由 O10 任一错误位、命令回读超时、非法硬件反馈、控制安全不变量失效或相关节点/连接重启触发的单侧不可自动恢复状态。进入时撤销操作者授权并停止新命令；人工清除前必须确认健康并用新鲜真实反馈重新初始化硬限制器，清除成功后仍须重新使能。
_Avoid_: 输入 stale、单指 IK 失败、自动重试

**清除锁存故障**：
操作者对单侧发起的同步控制操作。控制节点先撤销授权，再主动取得全零的新错误状态和一帧新鲜合法的真实关节位置，用其重新初始化硬限速器后才清除锁存；任一步失败均保持锁存和未授权。它不复位真实硬件错误，也不自动重新使能。
_Avoid_: 厂商故障复位、自动恢复、清故障并启动

**O10 主动关节状态**：
由 O10 控制入口直接命令的 10 维关节状态，也是手部重定向节点唯一允许自主求解和输出的机器人状态。
_Avoid_: O10 全关节状态、16 自由度命令

**O10 完整运动学状态**：
用于计算连杆和指尖位姿的 16 维关节状态，由 10 维主动关节状态和依据目标侧机械耦合关系唯一派生的 6 维被动关节状态组成。
_Avoid_: O10 控制命令、16 个独立关节变量

**逻辑手侧**：
控制链路中的 `left` 或 `right` 身份；两侧能够同时工作，也允许只有一侧存在，并且一侧失败不阻断另一侧。
_Avoid_: 设备 ID、CAN 通道、USB 路径

**输入失效保持**：
实时人体手部输入断流、过期或连续转换失败后，系统停止产生新的机器人目标，使 O10 保持最后目标并显式报警。恢复必须由连续整侧有效帧和最短观测时间共同确认；确认期间使用独立候选区，恢复成功后先重发 stale 前保持目标、重置软滤波时间，再从下一帧低通追赶实时目标。它不是自动张手、自动关闭驱动或无约束跳到恢复后的首帧目标。
_Avoid_: 自动回零、断流急停
